#www Domain master configuration file


server {
    listen       80 default_server;
    server_name  localhost;
    index index.html index.htm index.php;

    access_log /data/logs/nginx/localhost.log compression;

    return 403;
}
